<%
  # Panel component
  # -> For short content in scrolling sections. Meant to be full-width.

  id ||= false # STRING: id attribute for the container, for anchor links etc.
  classes ||= false # STRING: additional modifier classes for the container
  borders ||= [ 'top' ] # STRING/ARRAY: sides you want borders on - see border_classes helper in config.rb
  el ||= 'section' # What HTML element should the wrapper be?
  padding ||= { x: 'wide', y: 'xwide' } # STRING/HASH: outside padding - see padding_classes helper in config.rb
  theme ||= 'default' # STRING: name of color theme, e.g. "reverse" or "wash"
  painted ||= false # NUMBER: use a painted background texture? pass in 1-9 to pick a variation

    # border_classes(borders),

  wrapper = [
    ("c-theme-#{theme}" if theme != 'default'),
    ("c-bg-painted-0#{painted}" if painted)
  ]

  utils = [
    'border-markers-wide-top',
    padding_classes(padding),
    (classes if classes)
  ]
%>

<<%= el %><%= class_list(wrapper) %><%= " id='#{id}'" if id %>>
  <div<%= class_list(utils) %>>
    <%= yield %>
  </div>
</<%= el %>>
