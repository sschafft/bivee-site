<%#
    Panel component
    - For short content in scrolling sections. Meant to be full-width.

    Locals:
    - STRING slug (optional): a slug header for the top of the panel, e.g. "Recent work"
    - STRING theme (optional): name of color theme, e.g. "reverse" or "wash"
    - ARRAY borders (optional): sides you want borders on. Pass "none" for no borders.
    - STRING classes (optional): additional modifier classes for the container

    Styles:
    core/borders
    core/color
    base/type
    layout/gutter
%>

<%
    slug ||= false
    theme ||= "default"
    classes ||= false
    borders ||= [ "top", "right" ]

    # make a list of border classes from the borders array
    border_list = ""

    if borders != "none"
        borders.each do |side|
            border_list << " border-#{side}"
        end
    end
%>

<section class="l-padding-top-wide l-padding-bottom-wide<%= border_list %><%= " c-theme-#{theme}" if theme != "default" %><%= " #{classes}" if classes %>">
    <% if slug %>
        <span class="t-heading t-heading--slug t-scale-zeta t-centered"><%= slug %></span>
    <% end %>
    <%= yield %>
</section>
