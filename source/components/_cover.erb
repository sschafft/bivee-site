<%
  # Cover component
  # - A 'hero' with a full-bleed image and headline

  # Locals:
  headline ||= false # STRING: a headline, if you'd like one
  bg_image ||= false # STRING: image for the background
  theme ||= "wash" # STRING: color theme
  classes ||= false # STRING: additional utilitiy classes you'd like to pass in
  padding ||= 'wide' # STRING/HASH: outside padding - see padding_classes in config.rb
  borders ||= false # STRING/ARRAY: borders - see border_classes in config.rb

  utils = [
    "c-theme-#{theme}",
    classes
  ]
%>

<div data-ui-cover <%= class_list(utils) %><%= " style='background-image: url(#{bg_image});'" if bg_image %>>
  <div data-ui-cover-headline class="<%= padding_classes(padding) %>"><%= yield %></div>
</div>
