<%#
    Logo component
    - use the <picture> element to place an SVG or PNG image, depending on support

    Locals:
    - STRING filename: the file; SVG and PNG must be identically named.
    - STRING classes (optional): additional modifier classes, as needed.
    - STRING alt (optional): alt tag content.

    Styles:
    - components/logo
%>

<%
    filename ||= "logo-bivee-reverse"
    classes ||= "logo"
    alt ||= "Bivee"
%>

<picture class="<%= classes %>">
    <!--[if IE 9]><video style="display: none;"><![endif]-->
    <source type="image/svg+xml" srcset="<%= asset_url config[:images_dir] + '/' + filename + '.svg'%>">
    <!--[if IE 9]></video><![endif]-->
    <img src="<%= asset_url config[:images_dir] + '/' + filename + '.png'%>" alt="<%= alt %>" title="<%= alt %>" />
</picture>
