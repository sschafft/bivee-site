<%#
    Staff Portrait component
    - A scaled photo of a team member

    Locals:
    - STRING image: the photo to display
    - STRING classes (optional): additional modifier classes, if needed

    Styles:
    - components/gallery
%>

<% classes ||= false %>

<div class="l-centered-block" style="max-width: 424px;">
	<div data-ui-aspect="1-2" class="-margin-bottom-narrow border-round<%= " #{classes}" if classes %>">
    <span data-ui-aspect-content class="c-well" style="background-image: url('/assets/images/portraits/<%= image %>');"></span>
	</div>
</div>
