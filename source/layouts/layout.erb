<!doctype html>
<html class="no-js" lang="">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="x-ua-compatible" content="ie=edge">

        <!-- Use title if it's in the page frontmatter -->
        <title><%= data.site.title %><% if current_resource.data.title.present? && current_resource.data.title != "home" %> | <%= current_resource.data.title %><% end %></title>

        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="apple-touch-icon" href="apple-touch-icon.png">

        <%= stylesheet_link_tag "vendor/normalize.css", "main" %>
    </head>

    <body>
        <header class="l-wrapper" role="banner">
            <a href="/">Bivee</a>
            <!-- <nav>
                <ul>
                    <li><%= link_to "Case Studies", "case_studies" %></li>
                </ul>
            </nav> -->
        </header>

        <%= yield %>

        <footer class="l-wrapper" role="contentinfo">
            <a href="/">Bivee</a>
            <small>&copy; Copyright <%= Time.now.year %> Bivee, Inc. All rights reserved.</small>
        </footer>

        <%= javascript_include_tag "main" %>
    </body>
</html>
